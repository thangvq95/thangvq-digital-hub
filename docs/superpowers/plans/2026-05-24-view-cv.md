# View CV Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Update the portfolio resume button label to "View CV" and ensure clicking it opens the newly placed `/resume.pdf` in a new tab.

**Architecture:** The resume PDF has been copied to the `public/` directory as `resume.pdf`. Next.js serves all files in `public/` statically at the root path `/`. We will update the HTML link in the `HeroSection` component.

**Tech Stack:** Next.js, React, Tailwind CSS

---

### Task 1: Update HeroSection CV Button Label

**Files:**
- Modify: `components/portfolio/HeroSection.tsx`

- [ ] **Step 1: Modify HeroSection.tsx**

Update lines 60–70 in `/components/portfolio/HeroSection.tsx` to change the text from "Download CV" to "View CV". Keep `href` pointing to `"/resume.pdf"`.

```typescript
          <a
            href="/resume.pdf"
            id="hero-download-cv"
            target="_blank"
            rel="noopener noreferrer"
            className="px-8 py-3 rounded-full font-medium glass transition-all duration-200 hover:-translate-y-0.5 cursor-pointer"
            style={{ color: "var(--text-primary)", border: "1px solid var(--border)" }}
          >
            View CV
          </a>
```

- [ ] **Step 2: Run verification steps (lint & build)**

Run: `npm run lint` and `npm run build`
Expected: Succeeds without errors.

- [ ] **Step 3: Commit the changes**

```bash
git add components/portfolio/HeroSection.tsx
git commit -m "feat: change download CV button to view CV"
```
